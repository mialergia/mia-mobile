import { StyleSheet } from 'react-native';

import { PIXEL_HEIGHT } from 'constants/app';

const styles = StyleSheet.create({
  container: {
    marginHorizontal: '10%',
  },
  buttonContainer: {
    marginTop: PIXEL_HEIGHT * 0.09,
  },
});

export default styles;
