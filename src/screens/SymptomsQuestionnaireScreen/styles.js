import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },

  flatList: {
    paddingBottom: 100,
  },

  button: {
    flex: 1,
    position: 'absolute',
    bottom: 0,
  },
});

export default styles;
